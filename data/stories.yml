version: "2.0"
rules:
  - rule: get_started
    steps:
      - intent: phrase_presentation
      - action: action_send_tree

  - rule: send tree
    steps:
      - intent: send_tree
      - action: action_send_tree

  - rule: send full tree
    steps:
      - intent: send_full_tree
      - action: action_send_full_tree
